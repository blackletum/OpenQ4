### Highlights

- Restores working flashlight controls. Both direct impulse binds such as `_impulse50` and `bind f flashlight` now behave correctly again.
- Ships the full loadscreen set inside `baseoq4/pak0.pk4`, so fresh installs include the artwork without depending on loose overlay files.
- Keeps the replacement `v0.1.010` release aligned with the original platform package set while correcting the flashlight and packaging regressions.

### Upgrade Notes

- This replacement build supersedes the removed `v0.1.010` release and tag while keeping the same release version and platform archive lineup.
- Replace any earlier `v0.1.010` download with this build to pick up the corrected flashlight behavior and the complete packaged loadscreen assets.
- No bind migration is required if you already use `_impulse50`; the standard `flashlight` command path is restored as well.

### Change Log

- Removes the conflicting word-based impulse alias layer that was interfering with flashlight input.
- Restores a dedicated `flashlight` console command and keeps default bindings on canonical impulse commands.
- Promotes the loadscreen art set to canonical packaged content and includes the full directory in shipped release archives.
- Updates release validation so the loadscreen assets are expected inside the PK4 payload rather than as loose staged files.
